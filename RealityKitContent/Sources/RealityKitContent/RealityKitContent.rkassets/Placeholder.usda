#usda 1.0
(
    defaultPrim = "Root"
    metersPerUnit = 1
    upAxis = "Y"
)

# Placeholder scene so the .rkassets bundle is non-empty and compiles. Replace /
# extend this in Reality Composer Pro: author one environment per screen-size
# preset (e.g. "CinemaMedium", "CinemaLarge", "CinemaIMAX"), each with a
# DockingRegion at that screen width and a Reflection_Specular floor. The app
# loads them by name via Entity(named:in: realityKitContentBundle).
def Xform "Root"
{
}
